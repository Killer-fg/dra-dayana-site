const WA='https://wa.me/5521982148015?text='+encodeURIComponent('Olá! Conheci o trabalho da Dra. Dayana pelo site e gostaria de agendar uma avaliação.');document.querySelectorAll('[data-wa]').forEach(a=>{a.href=WA;a.target='_blank';a.rel='noopener noreferrer'});const header=document.querySelector('.site-header');const menuButton=document.querySelector('.menu-toggle');const menu=document.querySelector('#mobile-menu');function closeMenu(){if(!menu||!menuButton)return;menu.hidden=true;menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Abrir menu')}if(menuButton&&menu){menuButton.addEventListener('click',()=>{const open=menu.hidden;menu.hidden=!open;menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')});menu.addEventListener('click',e=>{if(e.target.closest('a'))closeMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!menu.hidden){closeMenu();menuButton.focus()}})}addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>24),{passive:true});if('IntersectionObserver'in window){const nav=[...document.querySelectorAll('.desktop-nav a')];const secObs=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)nav.forEach(link=>link.hash==='#'+entry.target.id?link.setAttribute('aria-current','location'):link.removeAttribute('aria-current'))}),{rootMargin:'-15% 0px -65% 0px'});document.querySelectorAll('main section[id]').forEach(s=>secObs.observe(s));if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const motion=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-revealed');motion.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.section-title,.final-cta h2').forEach(el=>{el.classList.add('scroll-reveal');motion.observe(el)});document.querySelectorAll('.about-photo,.hero-portrait').forEach(el=>{el.classList.add('image-reveal');motion.observe(el)})}}const track=document.querySelector('#review-track');if(track){const cards=[...track.querySelectorAll('.review')];const dots=[...document.querySelectorAll('[data-review]')];let active=0;const sync=()=>{dots.forEach((d,i)=>i===active?d.setAttribute('aria-current','true'):d.removeAttribute('aria-current'));document.querySelector('#review-status').textContent=`Depoimento ${active+1} de ${cards.length}`};const go=i=>{active=(i+cards.length)%cards.length;const card=cards[active];track.scrollTo({left:card.offsetLeft-cards[0].offsetLeft,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});sync()};document.querySelector('.review-prev')?.addEventListener('click',()=>go(active-1));document.querySelector('.review-next')?.addEventListener('click',()=>go(active+1));dots.forEach((d,i)=>d.addEventListener('click',()=>go(i)));track.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();go(active+1)}if(e.key==='ArrowLeft'){e.preventDefault();go(active-1)}});sync()}